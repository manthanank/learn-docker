# Junie Guidelines - learn-docker

## Directives
- Follow clean architecture principles.
- Use explicit types, type guards, and immutable return structures.
- All algorithms must be deterministic and testable without active root/daemon permissions.
- Validate compose dependencies using topological sort algorithms with cycle detection.
